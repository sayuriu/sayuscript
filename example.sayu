import extern "silly.c" as Silly;

import Sayuri::Engine::{
    Renderer::RenderEngine::{
        RenderStages,
        RenderStyle as _RenderStyle
    },
    Synthesizer as _Synthesizer
};

action main() -> {
    let a = (1 + 2 as Int as Float * 3 as Float);
}