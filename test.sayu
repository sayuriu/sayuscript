// Comment
let var expr = 1 + (-2 + 3) * -4;
let result = io(1, (-2 + 3) * -4, -4);
let float = -.1 + 2. + 3.e3 + 3e+4 + 1.5e-6;
let chained = io(otherFn(1, lucky()), 3, 4);
let tuple = (1, 2, 3);
let mulSum = action(m) -> { action(a, b) -> {m * (a + b)} };
action sumTriple(a, b, c) -> {
	let a2 = 3 * a;
	2
}